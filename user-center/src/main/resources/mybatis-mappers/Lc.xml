<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.user.dao.TaskDao">

	<select id="selectLcTaskList" resultType="map" parameterType="map">
		WITH RECURSIVE dict AS ( SELECT * FROM sys_department WHERE id =#{deptId} UNION ALL SELECT sys_department.* FROM sys_department, dict WHERE sys_department.parentid = dict.ID )
		SELECT ID,
			   dname,
			   parentid
		FROM
			dict
	</select>
</mapper>