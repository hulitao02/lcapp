<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.user.dao.UserPositionDao">

    <update id="update">
        update user_position
        <set>
            <if test="name != null and name != ''">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="positionDesc != null and positionDesc != ''">
                positiondesc = #{positionDesc,jdbcType=VARCHAR},
            </if>
            <if test="creator != null">
                creator = #{creator,jdbcType=INTEGER},
            </if>
            <if test="updateTime != null">
                updateTime = #{updateTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

    <sql id="where">
        <where>
            <if test="name != null and name != ''">
                <bind name="_name" value="'%' + name + '%'"/>
                and t.name like #{_name}
            </if>
        </where>
    </sql>
    <select id="count" resultType="int">
        select count(*) from user_position t
        <include refid="where" />
    </select>

    <select id="findData" resultType="UserPosition">
        select * from user_position t
        <include refid="where" />
        order by t.id desc
        limit #{length} OFFSET #{start}
    </select>
</mapper>
