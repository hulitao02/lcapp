<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.user.dao.UserRoleDao">

	<delete id="deleteUserRole">
		delete from sys_role_user
		<where>
			<if test="userId != null">
				and userId = #{userId}
			</if>
			<if test="roleId != null">
				and roleId = #{roleId}
			</if>
		</where>
	</delete>
	<!-- 查询用户所拥有的知识点权限-->
	<select id="getUserKpIds"  resultType="map" >
		SELECT DISTINCT
		kp_id
		FROM
		role_kp
		WHERE
		role_id IN ( SELECT roleid FROM sys_role_user WHERE 1 = 1 AND userid in
			<foreach collection="userIds" index="index" item="userId" open="(" separator="," close=")" >
				${userId}
			</foreach>
		)
	</select>
</mapper>